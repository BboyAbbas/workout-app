name = "workout-sync"
main = "src/worker.js"
compatibility_date = "2024-11-01"

# Stores one JSON document per user key: state:<id> -> { data, updatedAt }
[[kv_namespaces]]
binding = "WORKOUT_KV"
id = "e2ecc9528c8049c0a7900e34d5e55878"

# SYNC_TOKEN is set as a secret (wrangler secret put SYNC_TOKEN), never committed.
